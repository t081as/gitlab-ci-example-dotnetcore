stages:
    - build

release:
    stage: build
    only:
        - tags
    artifacts:
        name: "${CI_PROJECT_NAME}-${CI_COMMIT_TAG}"
        paths:
          - ./*.zip
    script:
        - ./build --configuration Release --buildnumber $CI_PIPELINE_IID
debug:
    stage: build
    script:
        - ./build